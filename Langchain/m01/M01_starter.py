"""
Milestone 01 — First LLM Call
==================================
GOAL: Connect to Groq and get your first LLM response.

INSTALL (run in terminal):
  pip install groq langchain-groq python-dotenv

CREATE .env file in this folder:
  GROQ_API_KEY=your_key_here
  (Get free key at console.groq.com)

TASKS:
  1. Load environment variables from .env
  2. Create a Groq LLM object using ChatGroq
  3. Define a messages list with one user message
  4. Call .invoke() on the LLM
  5. Print the response text to terminal

EXPECTED OUTPUT:
  LLM Response:
  LangChain is a framework for building applications powered by LLMs...

  Done!
"""

import os

# ── TASK 1: Load .env ─────────────────────────────────────────


# ── TASK 2: Create LLM object ────────────────────────────────
#  from langchain_groq import ChatGroq


# ── TASK 3: Define messages ───────────────────────────────────
#  from langchain_core.messages import HumanMessage, SystemMessage


# ── TASK 4: Call .invoke() ────────────────────────────────────


# ── TASK 5: Print response ────────────────────────────────────
